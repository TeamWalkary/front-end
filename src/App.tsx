import styled from "styled-components";

export default function App() {
  return (
    <MobileContainer>
      <h1>라우터 설정 필요!</h1>
    </MobileContainer>
  );
}

const MobileContainer = styled.div`
  position: relative;

  margin: 0 auto;
`;
